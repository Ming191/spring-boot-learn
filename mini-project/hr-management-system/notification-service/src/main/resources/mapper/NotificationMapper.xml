<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="vn.amela.notificationservice.mapper.NotificationMapper">

    <insert id="insert" keyProperty="notification.id" useGeneratedKeys="true">
        INSERT INTO notifications (
            event_id,
            event_type,
            aggregate_type,
            aggregate_id,
            recipient_type,
            recipient_id,
            title,
            message,
            payload,
            is_read
        ) VALUES (
            #{notification.eventId},
            #{notification.eventType},
            #{notification.aggregateType},
            #{notification.aggregateId},
            #{notification.recipientType},
            #{notification.recipientId},
            #{notification.title},
            #{notification.message},
            #{notification.payload},
            0
        )
    </insert>

    <select id="existsByEventId" resultType="java.lang.Boolean">
        SELECT COUNT(1) > 0
        FROM notifications
        WHERE event_id = #{eventId}
    </select>

</mapper>
